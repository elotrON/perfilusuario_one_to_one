info:
  name: Nuevo usuario
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/usuarios"
  body:
    type: json
    data: |-
      {
        "nombre": "Aurelio pata",
        "email": "aurelio@fi.fi"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
