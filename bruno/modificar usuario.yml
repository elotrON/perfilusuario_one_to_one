info:
  name: modificar usuario
  type: http
  seq: 5

http:
  method: PUT
  url: "{{url}}/usuarios/4"
  body:
    type: json
    data: |-
      {
        "nombre": "paco paquito",
        "email": "pppp@pppp.in"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
