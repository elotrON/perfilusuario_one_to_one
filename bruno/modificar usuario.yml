info:
  name: modificar usuario
  type: http
  seq: 5

http:
  method: PUT
  url: "{{url}}/usuarios/6"
  body:
    type: json
    data: |-
      {
        "nombre": "pepe tambor",
        "email": "pepe@tampor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
