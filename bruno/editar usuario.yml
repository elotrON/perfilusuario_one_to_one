info:
  name: editar usuario
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{url}}/usuarios/7"
  body:
    type: json
    data: |-
      {
        "email": "assaasss@marta.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
