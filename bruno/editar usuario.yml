info:
  name: editar usuario
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{url}}/usuarios/1"
  body:
    type: json
    data: |-
      {
        "nombre": "aaaaaa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
