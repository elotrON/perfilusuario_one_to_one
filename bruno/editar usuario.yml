info:
  name: editar usuario
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{url}}/usuarios/5"
  body:
    type: json
    data: |-
      {
        "email": "mm@marta.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
